apiVersion: v2
name: liquidwar-play
description: Browser front-end to play/spectate the trained liquidwar policy (the GPU engine, served live over a websocket).
type: application
version: 0.1.0
appVersion: "0.5.0"
